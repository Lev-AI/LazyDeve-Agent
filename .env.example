

# LazyDeve Environment Configuration Template
# Copy this file to .env and replace with your actual values

# ===============================
# PUBLIC_AGENT_URL
# Public HTTPS URL exposed via Cloudflare Tunnel.
# Used by GPT Actions / external clients to access the agent API.
# ===============================
# PUBLIC_AGENT_URL=https://agent.lazydeve.com
PUBLIC_AGENT_URL=
PORT=8001
ENVIRONMENT=development

# ===============================
# OpenAI Configuration
# ===============================
OPENAI_API_KEY=

# ===============================
# Webhook Configuration (Optional)
# ===============================
# Used for async / event-driven workflows (RAG, MCP orchestration).
# Not required for GPT Actions or simple request/response API usage.
# ChatGPT Actions do NOT support incoming webhooks.
# GPT_WEBHOOK_URL is reserved for future custom GPT / LLM gateways.

WEBHOOK_URL=
GPT_WEBHOOK_URL=

# ===============================
# GitHub Configuration - *REPO_NAME- for developer auto-update after agent core updates
# ===============================
GITHUB_TOKEN=
GITHUB_USER=
REPO_NAME=

# ג… TASK 4.2 FIX: GitHub Access Control
# allow_github_access: Controls whether GitHub repository creation is enabled
#   true  = When creating a project, automatically create GitHub repository (local + GitHub)
#   false = When creating a project, create locally only (no GitHub repository)
#   Note: This controls the default behavior. You can still override via create_github_repo parameter

allow_github_access=true

# ===============================
# Additional Configuration
# ===============================
# Add any other sensitive data here

# Agent Introduction and Version
AGENT_INTRO="Hello, I am LazyDeve — your autonomous development agent. I self-update on every launch, sync with GitHub, and maintain local projects in full parity. You can interact with me via /execute or /ping-agent."
AGENT_VERSION="1.4.1"

# ===============================
# LLM PROVIDER CONFIGURATION
# ===============================
# ===================================
# Dynamic LLM Selection (Task 7.7.14)
# ===================================
# LLM_MODE: Control model selection mode
#   "auto"   = Automatically select model based on task type (default)
#              - Test/Refactor tasks → mistral-medium
#              - Analysis tasks → gemini-pro
#              - Generation tasks → gpt-4o
#              - Documentation tasks → claude-3.5-sonnet
#              - Default tasks → gpt-4o-mini
#   "manual" = Always use MANUAL_LLM model for all tasks
# 
# Example: LLM_MODE=auto  (intelligent model selection)
# Example: LLM_MODE=manual (force specific model for all tasks)
LLM_MODE=auto

# MANUAL_LLM: Model to use when LLM_MODE=manual
# Only used when LLM_MODE is set to "manual"
# Example: MANUAL_LLM=gpt-4o
MANUAL_LLM=gpt-4.1-mini

ACTIVE_LLM=openai

# OpenAI Models
OPENAI_MODEL=gpt-4.1-mini

# Anthropic Claude Models
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-4.5-sonnet

# Google Gemini Models
GEMINI_API_KEY=
GEMINI_MODEL=gemini-pro

# Mistral AI Models
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-medium

# For agent authentication (used by ChatGPT Apps)
API_BEARER_TOKEN=

# ============================================================
#  LazyDeve Authentication Control
# ------------------------------------------------------------
# ENABLE_AUTH controls whether Bearer-token authentication is active.
# When ENABLE_AUTH=false  Development mode (no token required)
# When ENABLE_AUTH=true   Production mode (Bearer token required)
#
# The token itself is defined below as API_BEARER_TOKEN.
# Make sure ChatGPT Actions YAML includes the same token value.
# ============================================================


ENABLE_AUTH=false

# ============================================================
# 🔐 AUTHENTICATION MODE
# ------------------------------------------------------------
# ENABLE_AUTH controls whether Bearer-token authentication is active.
# false = Development mode (no authentication required)
# true  = Production mode (Bearer token required on write endpoints)
#
# ✅ Before enabling auth (ENABLE_AUTH=true), make sure that:
# 1. Your ChatGPT Actions YAML includes the security schema:
#      securitySchemes:
#        BearerAuth:
#          type: http
#          scheme: bearer
#          bearerFormat: JWT
# 2. ChatGPT requests include the header:
#      Authorization: Bearer <API_BEARER_TOKEN>
# ============================================================



